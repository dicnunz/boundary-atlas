{
  "name": "boundary-atlas",
  "version": "0.1.1",
  "type": "module",
  "funding": {
    "type": "custom",
    "url": "https://nicdunz.gumroad.com/l/smrimu"
  },
  "bin": {
    "boundary-atlas": "./dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@boundary-atlas/core": "0.1.1",
    "commander": "^13.1.0"
  }
}
